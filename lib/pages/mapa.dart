import 'package:flutter/material.dart';

class Mapa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: new Text('Mapa de sitios para ejercitarce'),
      ),
      body: new Center(
        child: new Text('Codigo de implementación del mapa')
      ),
    );
  }
}